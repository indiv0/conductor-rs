stages:
  - template: azure/stages.yml@templates
    parameters:
      codecov_token: $(CODECOV_TOKEN_SECRET)
      minrust: nightly-2019-08-21
      benches: true
      rust: nightly
      nightly_coverage: true

resources:
  repositories:
    - repository: templates
      type: github
      name: indiv0/azure-pipelines
      endpoint: indiv0
